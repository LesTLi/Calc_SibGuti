language: C++ 
dist: xenial

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-7
      - g++-7
      - qt5-default

script:
  - export CC=gcc-7
  - export CXX=g++-7
  - qmake
  - make
  - ./test/tst_calctest
